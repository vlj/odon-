<Page
    x:Class="client.ConnectedPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:client"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel>
            <TextBlock x:Name="Token" />
            <TextBox x:Name="username" Header="Id (email)" InputScope="EmailSmtpAddress" />
            <TextBox x:Name="password" Header="Password" InputScope="Password" />
            <ProgressRing x:Name="ring" />
            <Button Content="Validate" HorizontalAlignment="Center" Click="Button_Click" />
        </StackPanel>
    </Grid>
</Page>
