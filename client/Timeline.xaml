<Page
    x:Class="client.Timeline"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:client"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Hub Header="Mastodon" x:Name="hub">
        <FlyoutBase.AttachedFlyout>
            <Flyout Placement="Full">
                <ListView x:Name="displaySearch" ItemsSource="{Binding}" ItemTemplate="{StaticResource account_template}" />
            </Flyout>
        </FlyoutBase.AttachedFlyout>
        <HubSection x:Name="timelinesection" Header="Timeline" MinWidth="500">
            <DataTemplate x:DataType="local:Timeline">
                <StackPanel>
                    <ListView ItemsSource="{x:Bind tootscol}"
                                      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                                      ScrollViewer.VerticalScrollBarVisibility="Visible"
                                      ItemTemplate="{StaticResource toot_template}"/>
                </StackPanel>
            </DataTemplate>
        </HubSection>
        <HubSection Header="Mentions">
            <DataTemplate>
            </DataTemplate>
        </HubSection>
    </Hub>
</Page>
