<Page
    x:Class="client.Timeline"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:client"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Hub Header="Mastodon" x:Name="hub">
        <HubSection x:Name="timelinesection" Header="Timeline">
            <DataTemplate x:DataType="local:Timeline">
                <ListView ItemsSource="{x:Bind tootscol, Mode=OneWay}"
                                      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                                      ItemTemplate="{StaticResource toot_template}"/>
            </DataTemplate>
        </HubSection>
        <HubSection Header="Mentions">
            <DataTemplate>
            </DataTemplate>
        </HubSection>
    </Hub>
</Page>
